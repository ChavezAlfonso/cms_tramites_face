import {
  evaluateHtml
} from '../../../helpers/evaluate-html';
import { module, test } from 'qunit';

module('EvaluateHtmlHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = evaluateHtml(42);
  assert.ok(result);
});
