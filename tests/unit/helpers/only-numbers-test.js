import {
  onlyNumbers
} from '../../../helpers/only-numbers';
import { module, test } from 'qunit';

module('OnlyNumbersHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = onlyNumbers(42);
  assert.ok(result);
});
