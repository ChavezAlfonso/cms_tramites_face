import {
  statefullButton
} from '../../../helpers/statefull-button';
import { module, test } from 'qunit';

module('StatefullButtonHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statefullButton(42);
  assert.ok(result);
});
