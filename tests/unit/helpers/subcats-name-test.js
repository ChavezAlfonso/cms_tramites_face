import {
  subcatsName
} from '../../../helpers/subcats-name';
import { module, test } from 'qunit';

module('SubcatsNameHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = subcatsName(42);
  assert.ok(result);
});
