import {
  statusTramite
} from '../../../helpers/status-tramite';
import { module, test } from 'qunit';

module('StatusTramiteHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statusTramite(42);
  assert.ok(result);
});
